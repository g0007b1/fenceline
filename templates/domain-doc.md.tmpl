# {{zoneTitle}} — domain doc

<!-- agent-ready:generated: {{zoneDir}} (commits: {{commits}}, bug-fix commits: {{fixes}}, authors: {{authors}}) -->

Why this doc exists: this area changes often and has a history of fixes. Read it before editing anything under `{{zoneDir}}`.
Fill it from `git log --oneline -30 -- {{zoneDir}}`, the tests in the directory, and the types — or ask an agent: `/agent-ready-domain-doc {{zoneDir}}`.

## Code map

<!-- TODO(agent-ready): the 3–8 files/modules that matter, one line each: what it owns. -->

## Do not break

<!-- TODO(agent-ready): invariants. "X must always …", "Y is the only place that …". These are the things past bugs were about. -->

## Current facts

<!-- TODO(agent-ready): how it actually works today (not how it should). Data flow, states, edge cases already handled. Numbers over adjectives. -->

## Known gaps / backlog

<!-- TODO(agent-ready): what is knowingly imperfect, so agents don't "fix" it by accident. -->
